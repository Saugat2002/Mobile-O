# Shared dependencies (Mac, Linux, Windows — inference + most scripts).
# Install PyTorch *before* this file on Mac (see requirements-mac.txt).
# For Linux CUDA *training*, also install requirements-train.txt after CUDA torch.

-e .

tokenizers
sentencepiece
shortuuid
transformers
accelerate
peft
pydantic
markdown2[all]
numpy
scikit-learn
einops==0.6.1
einops-exts==0.0.4
timm>=1.0.0,<2.0
ftfy
diffusers==0.35.2
datasets
tabulate
ninja
qwen_vl_utils
huggingface_hub
wandb
Pillow
psutil

# Optional UI / API (not needed for infer_* or Mac benchmark)
gradio==4.16.0
gradio_client==0.8.1
requests
httpx==0.24.0
uvicorn
fastapi
