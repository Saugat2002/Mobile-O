# Mobile-O — single install file for Mac (Apple Silicon, arm64)
#
#   conda create -n mobileo python=3.12 -y
#   conda activate mobileo
#   python -c "import platform; assert platform.machine()=='arm64', 'Use arm64 conda (not x86_64/Rosetta)'"
#   pip install -r requirements-mac.txt
#
# Then: python infer_image_generation.py --model_path checkpoints/final_merged_model_23620 ...

-e .

# PyTorch (arm64 wheels; MPS for image generation)
torch>=2.4.0,<2.9
torchvision>=0.19.0,<0.24
torchaudio>=2.4.0,<2.9

# Hugging Face stack (pinned so one install matches torch 2.4+)
transformers>=4.46.0,<5.0
accelerate>=0.33.0
peft>=0.11.0
diffusers==0.35.2
huggingface_hub>=0.24.0
tokenizers>=0.19.0
sentencepiece>=0.2.0
qwen_vl_utils

# Model / training utilities used at inference
numpy>=1.26.0
scikit-learn>=1.4.0
einops==0.6.1
einops-exts==0.0.4
timm>=1.0.0,<2.0
ftfy>=6.2.0
datasets>=2.18.0
tabulate>=0.9.0
ninja>=1.11.0
shortuuid>=1.0.0
pydantic>=2.0.0
Pillow>=10.0.0
psutil>=5.9.0

# Optional logging / UI (safe on Mac; not required for infer_*)
wandb>=0.16.0
markdown2[all]>=2.4.0
gradio==4.16.0
gradio_client==0.8.1
requests>=2.31.0
httpx==0.24.0
uvicorn>=0.27.0
fastapi>=0.109.0

# NOT included (Linux CUDA training only): xformers, flash-attn, deepspeed, bitsandbytes
